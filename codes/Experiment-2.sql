-- i
UPDATE PRODUCT_MASTER
SET SELL_PRICE = 1150
WHERE DESCRIPTION = '1.44floppies';

-- ii
DELETE CLIENT_MASTER
WHERE CLIENT_NO = '0001';

-- iii
UPDATE CLIENT_MASTER
SET CITY = 'Bombay'
WHERE CLIENT_NO = '0005';

-- iv
UPDATE CLIENT_MASTER
SET BAL_DUE = 1000
WHERE CLIENT_NO = '0001';

-- v
SELECT *
FROM PRODUCT_MASTER
WHERE SELL_PRICE > 1500;
UPDATE PRODUCT_MASTER
SET SELL_PRICE = SELL_PRICE * 15
WHERE SELL_PRICE > 1500;
SELECT *
FROM PRODUCT_MASTER
WHERE SELL_PRICE > 1500;

-- vi
SELECT *
FROM CLIENT_MASTER
WHERE SUBSTR(CITY, 2, 1) = 'a';

-- vii
SELECT NAME
FROM CLIENT_MASTER
WHERE SUBSTR(NAME, 2, 1) = 'a';

-- viii
SELECT *
FROM PRODUCT_MASTER
ORDER BY DESCRIPTION;

-- ix
SELECT COUNT(*) TOTAL_NO_OF_ITEMS
FROM PRODUCT_MASTER;

-- x
SELECT ROUND(AVG(SELL_PRICE), 2), ROUND(AVG(COST_PRICE), 2)
FROM PRODUCT_MASTER;

-- xi
SELECT MIN(SELL_PRICE), MIN(COST_PRICE)
FROM PRODUCT_MASTER;

-- xii
SELECT MAX(SELL_PRICE) MAX_SELL, MAX(COST_PRICE) MAX_COST, MIN(SELL_PRICE) MIN_SELL, MIN(COST_PRICE) MIN_SELL
FROM PRODUCT_MASTER;

-- xiii
SELECT COUNT(*) FROM PRODUCT_MASTER WHERE COST_PRICE >= 1500;